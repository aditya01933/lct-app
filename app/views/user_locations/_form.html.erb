<%= form_with(model: user_location) do |form| %>
  <% if user_location.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_location.errors.count, "error") %> prohibited this user_location from being saved:</h2>

      <ul>
        <% user_location.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :lat, style: "display: block" %>
    <%= form.text_field :lat %>
  </div>

  <div>
    <%= form.label :long, style: "display: block" %>
    <%= form.text_field :long %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
